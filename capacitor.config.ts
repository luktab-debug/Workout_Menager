import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lukasz.trening',
  appName: 'Trening',
  webDir: 'www',
  backgroundColor: '#0f1115',
  android: {
    backgroundColor: '#0f1115'
  }
};

export default config;
